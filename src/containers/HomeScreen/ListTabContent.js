import React from 'react';

import LibraryTabContent from './LibraryTabContent';

const ListTabContent = ({navigation}) => (
  <LibraryTabContent navigation={navigation} section="list" />
);

export default ListTabContent;
