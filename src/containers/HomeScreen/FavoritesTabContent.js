import React from 'react';

import LibraryTabContent from './LibraryTabContent';

const FavoritesTabContent = ({navigation}) => (
  <LibraryTabContent navigation={navigation} section="favorites" />
);

export default FavoritesTabContent;
